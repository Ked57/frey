{
  "branches": [
    {
      "name": "master",
      "prerelease": "beta"
    },
    {
      "name": "main",
      "prerelease": "beta"
    }
  ],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md"
      }
    ],
    [
      "@semantic-release/npm",
      {
        "npmPublish": true
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md", "package.json", "package-lock.json"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/github",
      {
        "successComment": "🎉 This issue is included in version ${nextRelease.version} which is now available on [GitHub release](<github_release_url>) and [npm](https://www.npmjs.com/package/freyjs-test) 📦",
        "releasedLabels": ["released on ${nextRelease.gitTag}"]
      }
    ]
  ]
}
